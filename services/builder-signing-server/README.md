# builder signing server

Add this service only when you need Builder Program support.

Suggested responsibilities:
- keep builder credentials off the Codex runtime
- generate builder attribution headers remotely
- optionally mediate relayer actions
- authenticate callers before signing

Personal-trader mode does not need this component.
